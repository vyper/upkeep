h2
  ' Bike
  '  >
  = bike.name
  '  >
  ' Add part

= form_for :part, routes.bike_parts_path(bike.id) do
  - div class: 'input'
    - label :name
    - text_field :name

  - div class: 'input'
    - label :type_id
    / TODO: Improve
    - select :type_id, types

  - div class: 'controls' do
    - submit 'Create part'
